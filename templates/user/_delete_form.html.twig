<form method="post" style="display: inline-block;" action="{{ path('user_delete', {'id': user.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-square flat-btn btn-blue js-button">Delete</button>
</form>
